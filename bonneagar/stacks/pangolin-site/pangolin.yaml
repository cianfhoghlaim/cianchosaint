# =============================================================================
# PANGOLIN SITE — non-web-facing stack (NO Pangolin route)
# =============================================================================
# Generated by scripts/generate-stack-pangolin-site-yaml.ts on 2026-09-26.
#
# This stack is internal-only (the tunnel agent). It does not expose an
# HTTP surface itself; therefore it has no Pangolin route. The presence of
# this file satisfies stack-doctor's "must-have-pangolin-yaml" gate; the
# "noop:" block explicitly states that no route is needed.
#
# Per the 2026-09-26-pangolin-newt-v1.23-upgrade-v1 openspec change (Stage 2a).
# =============================================================================

pangolin:
  private-resources: {}
  noop: true
  noop-reason: "non-web-facing stack (the tunnel agent itself)"
