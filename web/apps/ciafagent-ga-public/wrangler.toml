name = "ciafagent-ga-public"
compatibility_date = "2026-08-23"
pages_build_output_dir = "dist"
main = "apps/web/app.config.ts"

# Worker bindings (apps/api)
[env.production]
  routes = [
    { pattern = "ga-public.cianchosaint.ie/*", zone_name = "cianchosaint.ie" },
    { pattern = "ga-public-api.cianchosaint.ie/*", zone_name = "cianchosaint.ie" }
  ]
  vars = {
    CONVEX_DEPLOYMENT = "prod:conic-ga-public"
    COPILOTKIT_RUNTIME_URL = "/api/copilotkit"
    ROOT_AGENT = "ga_root_agent"
    JURISDICTION = "ga"
    AUDIENCE = "public"
    PORTAL_PUBLIC_URL = "https://ga-public.cianchosaint.ie"
    CIANCHOSAINT_BUILD = "production"
  }

[env.staging]
  routes = [
    { pattern = "staging-ga-public.cianchosaint.ie/*", zone_name = "cianchosaint.ie" }
  ]
  vars = {
    CONVEX_DEPLOYMENT = "staging:conic-ga-public"
    COPILOTKIT_RUNTIME_URL = "/api/copilotkit"
    ROOT_AGENT = "ga_root_agent"
    JURISDICTION = "ga"
    AUDIENCE = "public"
    CIANCHOSAINT_BUILD = "staging"
  }
